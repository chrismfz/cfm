[global]
DEFAULT_EVERY   = "60s"
DEFAULT_TIMEOUT = "8s"
DEFAULT_COOLDOWN = "10m"

[exim_queues]
ENABLED          = 1
EVERY            = "60s"
TIMEOUT          = "8s"
QUEUE_TOTAL_MAX  = 500
QUEUE_FROZEN_MAX = 200
SAMPLE_LIMIT     = 10
COOLDOWN         = "10m"
