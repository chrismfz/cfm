# Endpoint scope inventory (backend pass)

This inventory documents the effective authz classification enforced by the backend:

- **scoped-allowed**: endpoint is allowed for scoped tokens, but must apply vhost/user scoping (`parseVhostFilter`, `vhostAllowed`, `scopeCheckHost`, or `scopedMySQLFilterHandler` derived filters).
- **admin-only**: endpoint rejects scoped tokens with `403` via `IsAdminRequest`/admin wrappers.

**The `nil` scope sentinel means admin/loopback ONLY.** `vhostScopeFromContext`
normalizes a *scoped*-role request that carries no scope map (e.g. a DB-only
viewer token whose vhost set is nil) to a non-nil **empty** set, so every
`nil == no restriction` consumer above (`vhostAllowed`, `parseVhostFilter`, the
`*ForScope` list filters, and the `validateScoped*` write guards) fails closed
for it — a vhost-less scoped token matches no host and sees no rows, rather
than being mistaken for admin. Code that reads `CtxScopeKey{}` **directly**
(bypassing that helper — today only `deriveScopedMySQLOwners`) must keep its own
`len(scope)==0` guard.

## `internal/webdetector/http_api.go` + related handlers

### Scoped-allowed

- `/api/v1/webdet/top-short`, `/api/v1/webdet/top` (`parseVhostFilter`)
- `/api/v1/webdet/suspicious` (`parseVhostFilter`)
- `/api/v1/webdet/long-top` (`parseVhostFilter`)
- `/api/v1/webdet/drilldown` (`vhostAllowed` + `parseVhostFilter`)
- `/api/v1/webdet/analyze-host` (`vhostAllowed`)
- `/api/v1/webdet/host-access-history` (`host_access_api.go`, `vhostAllowed`) — bounded ARCHIVAL traffic profile of one vhost from the edge's FULL access log (the `if=$log_main_request` access.log only — the focused `access.cfm.log` is never a candidate) plus rotated siblings via `edgelog.ScanHost`; optional `combine` section joins history-store challenge/WAF stats over EXACTLY the same absolute `[from,to)` window as the access scan — with `merge_www` across both twins (combined + `detector_by_host`; `detector_partial=true` if a twin query failed, and `detector_coverage` reports the store's real retention/oldest retained event so partial history never looks complete). Result is keyed to a single vhost, so a scoped token may profile only its own hosts — same host-param scope model as `analyze-host`; with `merge_www=1` the www/bare TWIN is a separate vhost key and must also be in scope (fail-closed 403). Expensive by nature (up to 60M log lines / 120s per call), so archive scans are capped node-wide at TWO concurrent (`429` + `Retry-After` beyond) and `hours` is clamped to ≤90 days at the API boundary before it reaches either the scan or the history queries. Policy note: responses include a few NODE-WIDE scan-evidence counters (`scanned`, `skipped_no_host`, `files_scanned`, bad-request section totals) — aggregate operational metadata with no foreign hosts/paths; acceptable for internal fleet credentials, strip for any future tenant-facing scoped use. Backs the MCP `host_access_history` tool.
- `/api/v1/webdet/vhosts` (`parseVhostFilterWithAliases` + `vhostAllowed`)
- `/api/v1/webdet/rules*` (scope helpers in `traffic_rules_api_handlers.go`)
- `/api/v1/webdet/history/events|summary|challenge-outcomes|waf-by-rule|vhost-overview` (`scopeCheckHost`). **Payload redaction on `history/events`:** rows are returned with their stored `payload_json` verbatim EXCEPT that `payload.sig` and `payload.ptr` are stripped for non-admin callers (`redactScopedHistoryRows`, `history_api_handlers.go`; the one key list is `scopedRedactedPayloadKeys`). `sig` is the ChallengeV2 Rung-1 device report (`hardwareConcurrency`, `deviceMemory`, `devicePixelRatio`, pointer/touch/key counts) that CFM's own challenge page collects from each visitor, added 2026-09-22. A tenant could measure the same values from their own site's JS, so it is not secret — but it is per-visitor browser-fingerprinting material CFM gathered, and the scoped surface previously carried nothing of the kind (the UA was the most it exposed), so the boundary default is closed. Nothing is lost operationally: the burn-in readout `sig` exists for is admin/MCP. `ptr` (added 2026-09-22, on `challenge_solved` and `challenge_v2_reject` rows) is the solving client's reverse DNS, and its stripping is **defence in depth, not a boundary**: scoped callers already get a per-IP PTR for their own vhosts from `/api/v1/webdet/drilldown` (`HostDetail`) and `/api/v1/webdet/analyze-host`, and anyone holding the IP can resolve it. It is stripped only so scoped history rows look as they did before these rows carried one — do not read it as "PTR is admin-only". (`enrich=1` stops at country/ASN to avoid blocking reverse DNS, not for privacy.) `country`/`country_iso`/`asn`/`asn_name` are NOT stripped: `enrich=1` already gives them to scoped callers. Scoped callers keep the rest of the payload (`hs`, `tells`, `v2`, `v2_waived`, `v2_waiver_miss`, `ua`, `solve_ms`, `tls_fp`, `country`, `asn`, …). Any future admin-only payload key goes in the same helper and this line.
- `/api/v1/challenge/vhost`, `/api/v1/challenge/events`, `/api/v1/challenge/vhost/add|remove|status|attack` (`vhostAllowed` checks). `vhost/add`: a SCOPED caller's TTL is clamped to `scopedMaxChallengeTTL` (24h) with `ttl_capped=true` in the response — the customer panic button is a temporary shield, not standing config; admin/loopback uncapped (slice D, 2026-09-22). `vhost/attack` is the Under-Attack operator override (`?host=&on=1|0` → `SetVhostAttackOverride`): a scoped token may force its own vhost into/out of UNDER_ATTACK (same trust model as manual challenge add/remove); a SCOPED `on=1` is TTL-bound to the same 24h ceiling as the panic-button arm (response carries `ttl`+`expires_at`; expiry returns the vhost to AUTO control, audited via `challenge_vhost_attack_override` with `payload.ttl_sec`) while admin overrides stay unbounded; returns `409` when `UNDER_ATTACK` is disabled. The read rows (`vhost`, `vhosts`, drilldown) carry an additive `state` (normal|suspicious|challenged|under_attack), stamped by the single `deriveVhostState` helper — no scope change.
- `/api/v1/challenge/exclude/*`, `/api/v1/waf/exclude/*` (`validateScopedExcludeWrite`: the unscoped-write role is the EXPLICIT admin role (`IsAdminRequest`), not a nil vhost scope — a non-admin caller with an empty/nil scope is denied (fail-closed), so a vhost-less DB-only scoped token cannot write global/out-of-scope excludes. Scoped tokens are otherwise limited to `type=host` within their vhost allowlist; list reads are scope-filtered. WAF add/remove also accept an admin-only `scope_hosts` qualifier via `effectiveExcludeScope` — likewise gated on `IsAdminRequest`: it can only *narrow* an admin-global entry to specific vhosts, and a scoped caller's effective scope is always pinned to its own context vhost set, so the param can never widen or redirect a scoped write)
- `/api/v1/challenge/access`, `/api/v1/challenge/access/get|add|update|remove` (`scopeAllowsVhosts` + `scopeFilterChallengeAccess`, shared with `/api/v1/webdet/rules/*` — see `challenge_access_api_handlers.go`). The Challenge Access-Control allow-list: entries EXEMPT matching requests from the interactive challenge (challenge→allow only; never softens a per-IP block, never touches the WAF). Every write (add/update) requires that ALL vhosts in `scope.vhosts` — both the existing entry's and the target's — are inside the token's vhost allowlist (`scopeAllowsVhosts`, fail-closed for nil/`*`/wildcard values a scoped token does not literally hold), and list reads are scope-filtered (`scopeFilterChallengeAccess`). Unlike the host-only `/challenge/exclude/*`, a scoped token MAY use the richer match dimensions (path/ua/country/ip/asn/method/verified_bot) — but they are always pinned under an in-scope vhost, so an entry can never affect another tenant. Admin/loopback (nil scope) may scope entries to any vhost incl. `*`.
- `/api/v1/clam/override/list|add|remove` (`validateScopedExcludeWrite`, `type=host` only: per-vhost ClamAV upload-scan override; a scoped token can flip only its own vhost, list reads are scope-filtered. The override XORs the vhost against the global `CLAM_SCAN_DEFAULT`)
- `/api/v1/clam/sigignore/list|add|remove` (`validateScopedSigIgnoreWrite`: per-signature ClamAV excludes — log-only downgrade. GLOBAL entries (`host` empty) are admin-only; a scoped token may add/remove/see only entries for hosts in its vhost scope. All writes and denied attempts audit-logged to `cfm.clam.log`)
- `/api/v1/clam/mode/list|add|remove` (`validateScopedExcludeWrite`, `type=host` only: per-vhost ClamAV scan-mode flip — XOR against the global `CLAM_SCAN_MODE`. A scoped token flips only its own vhost (inline blocks that vhost's own uploads — self-inflicted, same trust model as the scan toggle); list reads scope-filtered; writes + denied attempts audit-logged as `[clam_mode]`)
- `/api/v1/waf/engine/summary` (`vhostAllowed` filter — scoped callers see only their own vhosts' WAF-engine stats; verified by `authz_integration_test.go`)
- `/api/v1/http3/enable|disable` (`validateScopedHTTP3Write` host-scope check)
- `/api/v1/site-cache/list|get|set|remove|purge` (`scopeAllowsVhosts` + `scopeFilterSiteCache` — per-vhost edge caching opt-in, host-keyed; see `site_cache_api_handlers.go`). A scoped token may set/get/remove/purge only hosts inside its vhost allowlist (fail-closed for a nil/empty scope), and list reads are scope-filtered. `set` is a MERGE (only the fields in the body change) and records the opt-in attribution from the token, never the body: a scoped creator is stored as `scope_hosts=[host]` — exactly the host, never the token's whole allowlist (it used to be, which showed a tenant's full domain list to any other tenant whose scope held that host; legacy entries are trimmed on load). **`purge?all=1` is admin-only** (`RequireAdmin`): a global purge spans every tenant. Enabling a tier records INTENT only — the never-cache rails live at the edge and are not disableable here (see `docs/site-cache-design.md` §4).
- `/api/v1/site-cache/stats[?host=]` (`handleSiteCacheStats`, `site_cache_stats.go`): per-vhost edge cache counters. `?host=` outside the token allowlist is `403`; an in-scope `?host=` resolves only to a policy key inside the caller's scope (`SiteCacheStatsHost` — a scoped tenant drilling into its own `a.example.com` never gets an admin `*.example.com` row, which aggregates every sub-host under the pattern, other tenants' included); the unfiltered list is filtered with `vhostAllowed`, so a scoped caller sees only exact rows for its own hosts. Backs the MCP `site_cache_stats` tool.
- `/api/v1/waf/hit-rates` (`vhostAllowed` host-scope guard in `handleWAFHitRates`: a scoped caller must target one in-scope host — an empty host, which aggregates every tenant, and any out-of-scope host are `403`; admin/loopback may pass any host or none. Audit F02.)

### Admin-only

- `/api/v1/webdet/summary`
- `/api/v1/webdet/hot-ips`, `/api/v1/webdet/ip-short`, `/api/v1/webdet/ip-drilldown`, `/api/v1/webdet/analyze-ip`
- `/api/v1/webdet/history/stats|prune|truncate`
- `/api/v1/webdet/ua-top`, `/api/v1/webdet/ua-drill`, `/api/v1/webdet/ua-emergency` (global "Web Bots" UA controls — `RequireAdmin`)
- `/api/v1/clam/health` (`RequireAdmin`: box-level ClamAV scanner status — clamd reachability, circuit breaker, queue geometry, lifetime counters, 24h infection count (persisted), global scan default. Not per-vhost, so admin-only. Per-vhost scan coverage on the ClamAV page comes from the scoped `/api/v1/webdet/vhosts` instead.)
- `/api/v1/challenge/summary|vhosts|ips|ip`

## `internal/apiserver/apiserver.go` + token/mysql endpoints

### Scoped-allowed

- `/api/v1/mysql/user-summary|user-kills|user-history` via `scopedMySQLFilterHandler` (explicit `?user=` or derived scoped owners).
- `/api/v1/mysql/user-kill` (POST) via `scopedMySQLFilterHandler` — kills one connection/query; `handleUserKill` enforces the target `(user, db)` is within scope (`userDBMatch`); admin (no filter) may target any pid.
- `/api/v1/tokens/me` (self descriptor only; scoped callers only see their own token metadata).
- `/api/v1/mail/dns` (`system_status_endpoint.go`, `handleMailDNS`) — DNS mail-auth check (SPF/DMARC/DKIM/PTR/MX) for a `?domain=`. Admin (nil scope) may check any domain; a scoped caller must own the domain or a parent of it (`domainInScope` over the token's vhost set), else `403`. Records are public DNS, but the scope gate stops using the box as a general DNS-probe proxy. Sender IPs come from `selfip` (public only), not the caller.
- `/api/v1/mail/traffic` (`system_status_endpoint.go`, `mailTrafficScope` → `mailtraffic.TrafficSummary`) — Mail Monitor traffic view. Admin (nil scope) sees the whole server; a scoped caller is filtered to its own mail domains (`domain IN (allowlist)`), following the `nil=admin / non-nil=scoped / empty=owns-nothing` convention. Host-wide (`*`) and local-unix-user rows key to domain `*`, which is never in a vhost allowlist, so they are admin-only by construction (`totals.rejected`/`top_local_submitters` are empty for scoped); the scope helper also defensively drops `*` from a scoped allowlist. Ignores `?vhosts=` (scope comes only from the token). The `deliverability` block (per-provider delivered/deferred/bounced + top reasons) is **admin-only** — it is populated only when scope is nil, since remote-delivery lines are host-wide and not attributable to a tenant. Sibling mail reads `/api/v1/system/mail-queue` and `/api/v1/system/mail-log` remain `RequireAdmin`.

### Admin-only

- `/api/v1/mysql/state|processlist|top|locks|kills|history|history/events|history/summary|history/prune|history/truncate|history/timeline|cpu` via `adminOnlyHandler`.
- `/api/v1/auth/token` (issue token), `/api/v1/tokens/list`, `/api/v1/tokens/revoke`.
- `/api/v1/firewall/block` via `adminOnlyHandler` (global IP block).
- `/api/v1/firewall/block/batch` via `adminOnlyHandler` (bulk global IP block, ≤256 IPs/request; skips the server's own IPs and the calling admin's IP with per-IP `skipped` reasons).
- `/api/v1/firewall/list` via `adminOnlyHandler` (`firewall_list_endpoint.go`) — read-only dump of the manual/global block list with TTL + GeoIP; a node-wide list is meaningless to a scoped token.
- `/api/v1/firewall/counters` via `adminOnlyHandler` (`firewall_counters_endpoint.go`) — read-only nft named-counter view (per-rule match volume: portflood/connlimit/synflood/ppsflood/hardening/smtpblock), parsed from `ListTableJSON`; node-wide firewall internals.
- `/api/v1/firewall/selftest` via `adminOnlyHandler` (`firewall_selftest_endpoint.go`) — read-only nftlib backend self-diagnostics (EnsureBase timing split, per-set feed writes, `netlink` call counts/timeouts/slow calls); node-wide firewall internals.
- `/api/v1/firewall/path` via `adminOnlyHandler` (`netfilter_path_endpoint.go`) — read-only host-wide nftables base-chain order and NAT/redirect rules across CFM, Imunify/WebShield, iptables-nft, and other tables. Uses terse ruleset JSON so blocklist set elements are never returned; still exposes node-wide topology and is therefore admin-only.
- `/unblock` (POST) via `adminOnlyHandler` (`unblock_endpoint.go`) — removes a global nft block AND lays down a 24h allow-whitelist across every plane (nft, cfm.deny, csf, fail2ban, imunify, OpenResty/Lua WAF); host-wide state change with no per-vhost meaning. Consumers are admin-token callers (cfm-web fleet controller, `cfm` CLI). Was previously ungated behind mux-wide `TokenMiddleware`, so any authenticated scoped token could unblock+whitelist any IP.
- `/search` (GET) via `adminOnlyHandler` (`search_endpoint.go`) — read-only multi-source locate (nft/cfm.deny/csf/fail2ban/imunify) that enumerates where an arbitrary IP is blocked host-wide; cross-tenant recon with no per-vhost scoping. Was previously ungated behind mux-wide `TokenMiddleware`.
- `/debug/pprof/` and the named profilers `/debug/pprof/{cmdline,profile,symbol,trace}` via `adminOnlyHandler` (`apiserver_debug.go::registerPprofHandlers`) — Go runtime profiling (heap/goroutine/allocs/block/mutex dumps, CPU profile, execution trace, daemon command line). Host-global process introspection with no per-vhost meaning; a scoped tenant could otherwise pull a full heap dump of the daemon. Was previously ungated behind mux-wide `TokenMiddleware`, so any authenticated scoped token could reach it. `/debug/pprof/` (`pprof.Index`) fans out to all sub-profiles, so the single gate covers heap/goroutine/allocs/… too, and the `/cfm-admin/debug/pprof/` prefix re-dispatches onto the same mux and inherits it. `PprofWriteTimeoutMiddleware` (write-deadline relaxation for CPU/trace) is unchanged and runs outside the gate. Audit finding R02.
- `/api/v1/admin/authcheck` (admin-only auth probe for the edge `auth_request`; `RequireAdmin` → 200 for admin, 403 for scoped/anonymous; returns no data).
- `/api/v1/system/dnat`, `/api/v1/system/ssl/stats`, `/api/v1/system/ssl/refresh` (POST) and `/api/v1/health/{snapshot,timeseries,anomalies,ingest}` — all `RequireAdmin` (`system_status_endpoint.go`); back the dashboard's system/Node-health cards and the "Rescan certs" button.
- `/api/v1/system/edge-error-log` (`system_status_endpoint.go`, `handleSystemEdgeErrorLog` → `edgelog.TailError`) — `RequireAdmin`; bounded on-demand tail of the edge (OpenResty/Angie) ERROR log with optional grep. Host-wide (all vhosts share one error log), so admin-only by construction. Backs the MCP `edge_error_tail` tool. Same admin-only, allow-listed-path, bounded-tail family as the sibling on-demand log reads `/api/v1/system/ip-forensics`, `/api/v1/system/mysql-log`, and `/api/v1/system/mail-log`.
- `/api/v1/system/lve-cpu` (`system_status_endpoint.go`, `handleSystemLVECPU` → `lvecpu.Latest`) — `RequireAdmin`; per-tenant CPU pressure ranking on CloudLinux (every LVE's cores + %-of-limit from the in-memory `/proc/lve/list` sampler). Host-wide (all tenants across all resellers), so admin-only by construction. Backs the MCP `lve_cpu` tool. `available:false` off CloudLinux.
- `/api/v1/system/waf-fp-hunt` (`system_status_endpoint.go`, `handleSystemWAFFPHunt` → `edgelog.ScanError` + `panelfp.Summarize`) — `RequireAdmin`; scans the edge ERROR log for the panel LOGONLY markers (`[cfm_panel_waf]` / `[cfm_panel_decision]`) and returns node-wide aggregates (scanner-vs-non-scanner split, per-rule/verdict breakdowns, candidate FPs). Host-wide (one error log across all panel vhosts) and an enforcement-readiness view, so admin-only by construction. Backs the MCP `waf_fp_hunt` tool. Same admin-only, allow-listed-path, bounded-tail family as `/api/v1/system/edge-error-log`.
- `/api/v1/system/abuse-shadow` (`system_status_endpoint.go`, `handleSystemAbuseShadow` → `abuseshadow.ScanTail` + `abuseshadow.Summarize`) — `RequireAdmin`; tails the node-wide LOG-ONLY abuse-shadow log (`/var/log/cfm/cfm.abuse_shadow.log`, Signal C rate-outlier burn-in) and returns aggregates (would_challenge vs exempt_goodbot counts, top would-challenge (host,ip) outliers, datacenter-provider + verified-good-bot splits). Host-wide (one shadow log per node) enforcement-readiness view → admin-only by construction. Backs the MCP `abuse_shadow` tool. Same admin-only, bounded-tail family as `waf-fp-hunt`.
- `/api/v1/system/lsm-detections` (`system_status_endpoint.go`, `handleSystemLSMDetections` → `cfmlog.TailFile(which=lsm)` + `lsmdetect.Summarize`) — `RequireAdmin`; aggregates the node-wide cfm-lsm DETECT lines (per-policy counts + rate-cap suppression roll-up, top repeat offenders by policy×comm×exe). Kernel-level events carry no vhost notion, so admin-only by construction. Backs the MCP `lsm_detections` tool. Same admin-only, allow-listed-path, bounded-tail family as `/api/v1/system/cfm-log`.
- `/api/v1/system/lsm-status` (`system_status_endpoint.go`, `handleSystemLSMStatus` → `lsm.RunStatus(JSON)` + `Conf` allow-list accessors) — `RequireAdmin`; the `cfm lsm status --json` wire format (preflight, enabled, pinned BPF state, per-policy mode/runtime) augmented with `effective_allows` (merged allow_exe/allow_comm/allow_path per policy). Host-wide kernel/daemon state → admin-only by construction. Backs the MCP `lsm_status` tool.
- `/api/v1/system/config-drift` (`config_drift_endpoint.go`, `handleSystemConfigDrift` → `detectors.ReadSectionsFile` + `configdrift`) — `RequireAdmin`; stock (`/usr/share/cfm/configs/`) vs live (`/etc/cfm/`) config comparison (missing sections/keys = shipped features the live conffile never received). Host-wide config state → admin-only by construction. Backs the MCP `config_drift` tool.
- `/api/v1/detectors/coverage` (`detector_coverage_endpoint.go`, `handleDetectorsCoverage` → `meta.Catalog()` + `detectorstatus.GetSnapshot()` + `svcstat.Status` over a curated type→units affinity table) — admin-gated like its sibling `/api/v1/detectors/*` endpoints via `adminOnlyHandler`; daemon-vs-detector coverage matrix with host-reality verdicts. Backs the MCP `detector_coverage` tool.
- `/api/v1/detectors/source-resolution` (`detectors_api.go`, injected `detectorSourceReport` → `detectors.SourceReport` planners, GET-only) — admin-gated via `adminOnlyHandler` like its sibling `/api/v1/detectors/*` endpoints; dry-run source resolution per section (journald unit / file / docker container + reason, provisional/self-disable verdicts) using the SAME planners the registers wire with — probes run (journalctl/systemctl/docker/stat), nothing starts or changes. Host-wide source/probe surface → admin-only by construction. Backs `cfm detectors-srcresolve` (alias `detectors-resolve`), the cfm-admin "Source resolution" card, and the MCP `detectors_srcresolve` tool.
- `/api/v1/system/cpu-throttle` (`system_status_endpoint.go`, `handleSystemCPUThrottle` → `cputhrottle.ReadAndClassify`) — `RequireAdmin`; classifies whether high CPU load is thermal throttling / a frequency cap / genuine demand / idle / no-cpufreq (VM), from the box-wide cpufreq/thermal/utilization signals. Host-wide (whole-CPU state, not per-vhost), so admin-only by construction. Backs the MCP `cpu_throttle` tool.
- `/api/v1/system/cfm-log` (`system_status_endpoint.go`, `handleSystemCFMLog` → `cfmlog.TailFile`) — `RequireAdmin`; bounded on-demand tail of one of CFM's own `/var/log/cfm/*` logs (curated `which` allow-list; caller never supplies a path). `which=api` reads the canonical `cfm.api.log` auth/control-plane audit trail (current and legacy paths). Host-wide daemon/subsystem logs, so admin-only by construction. Backs the MCP `cfm_log_tail` tool. Same admin-only, allow-listed, bounded-tail family as `/api/v1/system/mysql-log` and `/api/v1/system/mail-log`.
- `/api/v1/system/journal` (`system_status_endpoint.go`, `handleSystemJournal` → `cfmlog.TailJournal`) — `RequireAdmin`; bounded `journalctl -n` tail of an **allow-listed** systemd unit (cfm + hosting-stack units; an arbitrary unit is rejected 400, so the read surface can't reach any unit on the box). Host-wide unit journals, so admin-only by construction. Backs the MCP `journal_tail` tool.
- `/api/v1/mail/runtime` (`system_status_endpoint.go`, `handleMailRuntime` → `mailruntime.Live` + `Discover{EximMaxima,SpamdMaxChildren}`) — `RequireAdmin`; SMTP/spamd runtime saturation snapshot (current inbound SMTP sessions vs `smtp_accept_max`, active spamd children vs `--max-children`, each as utilisation% + `ok`/`warn`/`critical`; `unknown` when a cap can't be resolved). Host-wide daemon capacity (whole-box SMTP listener + spamd pool, not per-vhost), so admin-only by construction. Backs the MCP `mail_runtime` tool.

## Edge-served admin endpoints (OpenResty / Angie)

Rendered entirely inside the edge proxy (Go cannot read nginx shdicts), so their
admin gate is delegated to Go via `auth_request` → `/api/v1/admin/authcheck`
(`RequireAdmin`). Present in **both** `configs/openresty.conf` and
`configs/angie.conf` (two server blocks each).

### Admin-only

- `/cfm-admin/lua-stats` (fleet-wide decision-cache / sslcache / WAF-exclude /
  WAF-rule-mode stats). The `auth_request` gate previously pointed at
  `/api/v1/tokens/me`, which is scoped-OR-admin, so a scoped cPanel viewer passed
  it and received the whole fleet's WAF excludes + rule tiers — a scoped-vs-admin
  boundary break. Repointed to the admin-only `/api/v1/admin/authcheck`. Audit F01.

